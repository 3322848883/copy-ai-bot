# 后端运行时依赖（M6 T6.4 生成）
fastapi==0.141.1
uvicorn[standard]==0.52.1
SQLAlchemy==2.0.52
asyncpg==0.31.0
pydantic==2.13.4
pydantic-settings==2.15.0
python-jose[cryptography]==3.5.0
bcrypt==5.0.0
cryptography==50.0.0
redis>=5.0,<9
celery==5.6.3
httpx==0.28.1
websockets==17.0.1
email-validator==2.3.0
python-multipart==0.0.32
python-dotenv==1.2.2
alembic==1.19.1
# alembic 同步迁移引擎需要 psycopg2（env.py 把 asyncpg URL 转同步驱动）
psycopg2-binary==2.9.12
# 真实信号源采集（M2 生产：Gate 带单广场浏览器采集）
playwright==1.57.0
# 链上支付（M4 T4.8：三链 USDT 确认数校验）
tronpy>=0.4
web3>=6.15
# 监控（M6 T6.2：/metrics Prometheus 文本格式）
prometheus-client>=0.20
